<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.diannaliset.vehicles">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".activities.a.mainActivityVehicleList.MainActivityVehiclesList"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".VehicleDriver" />
        <activity android:name=".ShowVehicle" />
        <activity android:name=".UpdateVehicle" />
        <activity android:name=".VehicleFeatures" />
        <activity android:name=".ShowVehicleFeatures" />
        <activity android:name=".VehiclesList"/>
        <activity android:name=".activities.a.showVehicle.ShowVehicle2"/>
    </application>

</manifest>